class AmrYadav:
    # get a variable a its value like a,b
    a=30
    b=40
    #
    # create a function this is also called METHOD
    # here we use a variable in function to access function by class or variable pass like 'a'
    def ffs(ss):
        # if we need to call the variables of class in method like 'a' & 'b' there for 
        #we use this way ss.a & ss.b
        print(20 + 30,ss.a)
# create a object to call  method (function) of class 
obj=AmrYadav()
obj.ffs()
    